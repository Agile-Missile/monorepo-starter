import { type DocsThemeConfig } from 'nextra-theme-docs';

const config: DocsThemeConfig = {
  darkMode: true,
  navigation: true,
  nextThemes: {
    defaultTheme: 'system',
  },
};

export default config;
